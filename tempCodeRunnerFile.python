from Fluxon.Connect import ConnectionHandler

connection = ConnectionHandler(host="localhost", port=8080)

# Example request
response = connection.send_request("new_user", {
    'username': 'test_user2',
    'password': 'Secure_password123@',
    'email': 'test_email2@mail.com'
})
print(response)
